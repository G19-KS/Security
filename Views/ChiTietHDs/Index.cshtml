@model IEnumerable<QuanLiKS.Models.ChiTietHD>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.HoaDon.SoHD)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PhongThue.MaThue)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SoNgayO)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Tien)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.HoaDon.SoHD)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PhongThue.MaThue)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SoNgayO)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Tien)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDCTHD }) |
            @Html.ActionLink("Tính tiền dm", "TinhTien", new { id= item.IDCTHD }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDCTHD })
        </td>
    </tr>
}

</table>
